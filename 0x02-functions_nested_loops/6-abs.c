#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @t: absolute value of an integer.
 * Return: The absolute value
 */
int _abs(int t)
{

	if (t >= 0)
		return (t);
	else
		return (-t);
}
