#include "main.h"
/**
 * add - funtion that adds two integers and returns the result
 * @a: number a
 * @b: number b
 *
 * Return: returns the result of addition
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}

/**
 * sub - subtraction
 * @a: the first number
 * @b: the next number
 * Return: tee diference from a to b
 */
int sub(int a, int b)
{
	int result;

	result = a - b;
	return (result);
}
/**
 * mul - function that multiplies two integers
 * @a: integer a
 * @b: integer b
 *
 * Return: retuns the multipliction of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * mod - function that multiplies two integers
 * @a: integer a
 * @b: integer b
 *
 * Return: retuns the multipliction of two numbers
 */
int mod(int a, int b)
{
	return (a % b);
}
