#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char s;

	int a;

	s = 'a';
	a = 0;
	while
		(a < 10) {
			putchar(a + '0');
			a++;
		}
	while
		(s <= 'f') {
			putchar(s);
			s++;
		}
	putchar('\n');
	return (0);

}
