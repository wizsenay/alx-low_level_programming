#include <stdio.h>
/**
 * main - Entry point
 *
 *Return: Always 0 (success)
 */

int main(void)
{
	char t;

	char a;

	t = 'a';
	a = 'A';
	while
		(t <= 'z') {
			putchar(t);
			t++;
		}
	while
		(a <= 'Z') {
			putchar(a);
			a++;
		}
	putchar('\n');
	return (0);
}
