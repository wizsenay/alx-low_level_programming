#include "main.h"
/**
 * mul - multiplies of two integers
 * @a: input number
 * @b: input number
 * Return: the out put of mul function
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;

	return (mult);
}
